# API 标准

每个接口记录系统、负责人、来源与版本、方法、路径、认证、权限、请求、响应及错误码。地址和示例按资料分级保存。

字段映射至少包含：源字段、目标字段、类型、单位、枚举、必填、默认值、空值、转换规则和来源。分页、排序、时区、金额精度和标识符口径应明确。

说明超时、重试、幂等、限流、部分失败、重复回调及兼容策略。契约待确认时标记 Unknown 并指定确认动作。

验证至少覆盖成功、字段缺失、非法枚举、权限不足、重复请求和依赖失败，按适用性记录结果。模拟样例与真实调用证据分别标记。
