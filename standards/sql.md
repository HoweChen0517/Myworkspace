# SQL 标准

SQL 附带业务问题、方言、Schema 来源、执行环境、参数、日期和数据口径。查询字段来自已核验定义。

明确粒度、主键、JOIN 基数、时间区间及时区、过滤、去重、空值、单位和分母。时间窗口优先使用左闭右开区间。示例参数与生产参数清晰标记。

分析默认使用授权的只读查询和有限样本。数据修改作为独立任务定义影响范围、恢复方案和执行授权。

验证记录：语法与方言、字段存在性、JOIN 前后行数、重复和缺失、聚合对账、边界样本。尚未连接数据库时，结果状态为 UNKNOWN，记录待执行步骤。
